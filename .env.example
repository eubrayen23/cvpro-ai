# ────── Supabase (OBRIGATÓRIO — client-side) ──────────────────────────────────
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# ────── App ───────────────────────────────────────────────────────────────────
VITE_APP_NAME=CVPro AI
VITE_APP_URL=http://localhost:5173

# ────── APIs de IA (SERVER-SIDE ONLY — set in Netlify environment variables) ──
# These must NOT use the VITE_ prefix. They are used only by the
# Netlify serverless function (netlify/functions/ai-proxy.js).
# Set them in Netlify Dashboard → Site Settings → Environment Variables.
#
# GEMINI_API_KEY=AIzaSy...
# GROQ_API_KEY=gsk_...
